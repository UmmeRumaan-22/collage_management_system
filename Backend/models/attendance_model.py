class Attendance:

    def __init__(self, attendance_id, student_id, event_id, status):
        self.attendance_id = attendance_id
        self.student_id = student_id
        self.event_id = event_id
        self.status = status